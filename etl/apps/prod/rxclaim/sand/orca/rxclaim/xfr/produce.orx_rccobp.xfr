
let string(1)[6] status = [vector "P", "X", "C", "R", "D", "Z" ];

out::length(in)=
begin
out:: length_of(status) ;
end;

out::normalize(in,index)=
begin
out.claimsts :: status[index] ;
out.* :: in.* ;
end;
