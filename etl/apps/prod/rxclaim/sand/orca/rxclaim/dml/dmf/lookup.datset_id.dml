// This uses a fixed size that evenly divides the block length in order to avoid partial records.
type lookup_dataset_id_t =
record
  decimal(27) dataset_id;
  string(100) uid;
  string(1) newline = "\n";
end;

metadata type = lookup_dataset_id_t;
